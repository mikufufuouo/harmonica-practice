{
  "name": "harmonica-lab",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run icons && tsc -b && vite build && node scripts/build-sw.mjs",
    "icons": "node scripts/generate-icons.mjs",
    "typecheck": "tsc -b --noEmit",
    "test": "tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "fake-indexeddb": "^6.2.5",
    "prettier": "^3.9.6",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  },
  "dependencies": {
    "tesseract.js": "^7.0.0"
  }
}
