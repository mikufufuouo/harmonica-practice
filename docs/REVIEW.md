# 阶段 0A review 记录

2026-09-14，主代理负责 review，Terra 实现。

## 审查重点

本轮交付是采样工具，不是识别器。首轮审查要求：

- 权限等待可取消；迟到的 getUserMedia 结果必须立即停止，不能覆盖新的会话。
- 初始化失败释放已取得的麦克风；音频挂起、轨道结束、页面隐藏/离开须中止采样。
- 录音开始冻结意图、调性与实际设置；导出包含版本、会话/采样 id，WAV 与 JSON 能配对。
- 时长取样本数；五秒硬上限、提前完成、取消必须明确区分。
- 回放与采集互斥，Object URL 可释放；界面不渲染每个 128-sample 回调。
- 常见 G/A/B♭ 琴的标准音区不能按 C 调向上转到错误八度。
- 标签始终为用户自报，不自动称为已确认单孔；虚拟琴标明“目标/意图”。

## 验证边界

自动化能验证数据格式、映射、构建和页面状态；没有真实口琴输入，就不能声明麦克风实吹质量、手机兼容性或串孔识别已通过。阶段 0B 才开展算法与真实数据验证。

## 本轮实际结果

- 主代理复跑 `npm run typecheck`、`npm test`（3 项）和 `npm run build`，全部通过。
- 外置 Chromium 页面正常加载；切换 G 调显示 1B=G3、4B=G4。
- 浏览器注入拒绝权限的测试替身后，点击开启显示 `NotAllowedError`，错误没有被停止提示覆盖，开启按钮恢复。
- 浏览器注入待决权限测试替身，点击开启、停止，再返回模拟轨道：模拟 track.stop 调用一次，页面保持停止状态。未访问真实麦克风；刷新已移除测试替身。
- 两轮代码 review 修复录音标签快照、取消代次检查、回放互斥、错误原因保留、会话/采样 id 区分、G/A/B♭ 音区及零样本拒绝。
- 尚未实测真实麦克风录制/回放/下载的完整往返、手机横屏布局、长期运行或设备中断。采样开始结束以 2048-sample 消息批次处理，边界可能包含批次和消息调度偏差；不能作为精确起音延迟测量工具。

结论：交付阶段 0A 采样原型供真实设备试用；不批准任何串孔识别准确性结论，也不进入整首评分。

## PWA 部署补充验收（2026-09-14）

用户要求上传 GitHub 供其部署。补充 manifest、PNG 图标、相对资源路径及生产 Service Worker。主代理复跑类型检查、3 项测试、构建，全部通过。

在本地静态服务器的 `/dist/` 子路径打开生产构建：Service Worker activated，192px 图标通过浏览器实际解码，manifest 与 worklet 返回正常。重新加载获得控制后关闭静态服务器，再次导航仍能显示应用，worklet 请求也由缓存返回 200。缓存按应用 scope 和内容哈希隔离；不主动跳过等待或刷新，不缓存用户录音。

此次仅验证桌面 Chromium 的离线外壳和子路径，不代表手机实机安装、音频权限或实吹已通过。GitHub 仓库为私有；由用户选择部署平台发布 HTTPS 站点。
