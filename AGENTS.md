# 口琴项目工作入口

## 新对话先做什么

1. 读取 [docs/STATE.md](docs/STATE.md) 和 [docs/DECISIONS.md](docs/DECISIONS.md)。这是最小接续包，不需要通读聊天或所有设计文档。
2. 查看 `git status --short --branch`、`git log -5 --oneline`；将实际 HEAD、未提交改动与 STATE 的代码基线核对。遇到差异先读相关 diff，不覆盖他人的工作，不把旧验收套到新代码上。
3. 按用户本轮要求和 STATE 的下一包继续；只读该任务相关的专题文档及源码。用户新要求优先于旧计划；没有新证据时沿用已记录决策，不重新做全项目设计。
4. 简短说明接续到哪一阶段、这一轮交付什么，再行动。短指令“继续口琴项目”即表示按此流程接续，不表示一次完成整个 roadmap。

## 工作方式

- 用户希望主代理（Astra）负责产品/技术设计、判断、review 和阶段取舍；实际 coding 交给 Terra，也可让 Luna 做边界清楚的小包。每包写清目标、范围、相关文件、验收与不做的内容。工具没有对应模型时如实说明，不能冒称由 Terra 实现。
- 一轮只完成一个可验收增量。实现完成、主代理 review、自动验证、用户实机验证是不同状态。真实音频门槛未过，不推出“串孔准确性已验证”或整首评分。
- 音频、谱图、用户曲库默认本地；公开仓库只放自造测试样本。用户已经授权此仓库公开及 GitHub Pages 部署，常规提交/推送/部署不必重复询问；此授权不包含公开用户素材。
- 一般代码改动执行 `npm run typecheck`、`npm test`、`npm run build`，并按改动做针对性检查；纯文档改动检查链接、事实和 diff 即可。测试范围和未测项必须如实写明。

## 上下文由谁维护、何时维护

- STATE 是唯一当前进度与交接处，由主代理维护。Terra/Luna 完成任务后回报：改动与行为、验证命令/结果及基线、未测项/风险、剩余工作；不要并行修改 STATE 或另建长期交接日志。
- 每次有实质代码/设计变化、收到实测反馈、阶段切换或即将换对话时，主代理在结束前更新 STATE 的相关几行。纯咨询且状态未变不强制更新。主代理 review 后才能将子代理的“已实现”提升为“已验收”。
- 中断任务时，将恢复点写入 STATE：目标、已做/未做、文件与未提交改动、已跑/未跑验证、下一条具体动作。完成后删掉恢复点；不要让终端 session ID、agent ID 或聊天引用成为继续工作的必要条件。
- 稳定取舍才写 DECISIONS：原因、证据/约束、重审条件。算法与 OCR 路线标注“待验证”，不冻结假设。改变决策时同步修订相关专题契约，用 Git 保留旧版本。
- 阶段结束只在 STATE 留结果、证据索引和下一阶段入口，不另写重复阶段总结。用户反馈记设备/版本/现象/复现情况，区分“用户报告”“已复现”“假设”；关闭的问题移出当前页。

## 防止膨胀与过时

- AGENTS 只放规则；STATE 放当前事实、未决问题、下一步；DECISIONS 放为什么；专题文档放详细契约/实验方法；Git 放过程历史。不在多处复制当前状态。
- 软上限：本文件 80 行，STATE 100 行，DECISIONS 12 项。超过时替换/压缩过期内容，专题细节移到已有文档；不机械截断仍需交接的信息。
- 验证条目带日期、代码版本和环境/范围。部署记录保留最近已确认的版本/运行链接，后续部署未查到结果就写未知；文档提交可晚于代码基线，不为修改自己的 SHA 无限追加提交。
- 已有研究按链接和结论复用。只有新失败样本、重审条件命中、依赖变化或时效核验需要时再研究，并记录改变了什么。
- 不复制完整日志、录音、谱图、密钥到上下文文档。项目接续不依赖某个模型的私人记忆或上一轮对话是否还在。

## 按任务读取

| 任务 | 进一步读取 |
|---|---|
| 产品范围/阶段门槛 | [PRODUCT](docs/PRODUCT.md) |
| 采集/识别/评估 | [ARCHITECTURE](docs/ARCHITECTURE.md)、[EXPERIMENT](docs/EXPERIMENT.md) |
| 歌曲结构/导入/指法 | [NOTE_SEQUENCE](docs/NOTE_SEQUENCE.md) |
| 图片校对/本地谱库/OCR | [IMAGE_IMPORT](docs/IMAGE_IMPORT.md)、NOTE_SEQUENCE |
| 运行/部署 | [README](README.md)、`.github/workflows/deploy-pages.yml` |

入口采用 Codex 的项目 AGENTS.md 发现机制；从本仓库开启新会话。若宿主未自动加载，明确读此文件即可，不需改全局配置。[官方说明，2026-09-14 核对](https://learn.chatgpt.com/docs/agent-configuration/agents-md)。
